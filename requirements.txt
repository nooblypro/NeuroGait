# NeuroGait ML & Runtime Dependencies
# Production Canonical Runtime: Linux aarch64 (Debian Bookworm / Python 3.11)
# Development Runtime: macOS Darwin (Apple Silicon / Python 3.11+)

# MediaPipe platform wheel selection:
# Linux x86_64 and aarch64 production container strictly pins 1.0.1 (TFLite XNNPACK CPU delegate).
# macOS darwin uses 0.10.35 to avoid Metal shader runtime crash.
mediapipe==0.10.35; sys_platform == "darwin"
mediapipe==1.0.1; sys_platform == "linux"

# Computer Vision & Video Decoding:
opencv-python-headless==5.0.0.93; sys_platform == "linux"
opencv-python-headless>=4.8.0; sys_platform == "darwin"

# Core Machine Learning & Scientific Stack:
scikit-learn==1.9.1
pandas==3.0.6
numpy==2.4.6; sys_platform == "linux"
numpy>=1.26.0,<3.0.0; sys_platform == "darwin"
joblib==1.6.0
scipy==1.17.1; sys_platform == "linux"
scipy>=1.11.0; sys_platform == "darwin"

# Testing & Application:
pytest>=8.0.0
streamlit>=1.35.0
requests>=2.31.0
